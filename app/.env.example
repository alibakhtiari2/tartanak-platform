# ── Tartanak Platform — Environment Template ──────────────────────────────────
# In Docker, inject all of these via environment: in docker-compose.yml

# Email (order notifications)
GMAIL_USER=
GMAIL_APP_PASSWORD=
EMAIL_FROM=
EMAIL_TEST_SECRET=

# Hub / Support portal (generate: openssl rand -hex 32)
HUB_SECRET=
ACCOUNTING_API_URL=
ACCOUNTING_API_KEY=
